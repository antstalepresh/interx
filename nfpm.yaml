name: "interx"
arch: "${ARCH}"
platform: "linux"
version: "${VERSION}"
section: "default"
priority: "extra"
replaces:
- interxd
provides:
- interxd
maintainer: "KIRA <partners@kira.global>"
description: "INTERX is an interchain engine, proxy, load balancer & security gateway service for communication between backend and frontend. It connects to KIRA sentry & validator nodes using the GRPC & RPC endpoints"
homepage: "http://github.com/KiraCore/interx"
license: "AGPL-3.0 license"
contents:
- src: ./bin/${ARCH}
  dst: ./bin/
